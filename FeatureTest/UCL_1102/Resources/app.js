var win = Ti.UI.createWindow({
	backgroundColor:'black',
	width:Ti.UI.SIZE,
	height:Ti.UI.SIZE
});

var view = Ti.UI.createView({
	backgroundColor:'white',
	width:2,
	height:2
});

win.add(view);
win.open();
